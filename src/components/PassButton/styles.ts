import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({ 

  buttonText:{
    backgroundColor: '#FFE608',
    width: 200,
    color: 'black',
    fontWeight: 'bold',
    fontSize: 15,
    height: 50,
    borderWidth: 1,
    borderRadius: 5,
    padding: 10,
    textAlign: 'center',
    margin: 14
  }
});