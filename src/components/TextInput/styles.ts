import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {},

  input:{
    backgroundColor: '#FFE608',
    width: '100%',
    color: 'black',
    fontWeight: 'bold',
    fontSize: 20,
    height: 50,
    borderWidth: 1,
    borderRadius: 5,
    padding: 10,
    textAlign: 'center'
  }


});